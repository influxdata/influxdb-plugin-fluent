<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>
<source>
  @type monitor_agent
  bind 0.0.0.0
  port 24220
</source>
<match *.**>
  @type copy
  <store>
    @type stdout
  </store>
  <store>
    @type influxdb2
    url http://influxdb:9999
    token my-token
    bucket my-bucket
    org my-org
    use_ssl false
  </store>
</match>